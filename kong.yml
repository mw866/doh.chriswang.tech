_format_version: "2.1"
_transform: true

services:
- name: doh
  url: https://cloudflare-dns.com/dns-query
  tags:
  - doh
  routes:
  - name: doh-route
    paths:
    - /
  plugins:
  - name: prometheus
